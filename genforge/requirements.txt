# GenForge v0 — pipeline + service (Python 3.10+)
pillow>=9.0
numpy>=1.24        # normal_gen, hifi
jsonschema>=4.0
fastapi>=0.100
uvicorn>=0.20
pydantic>=2.0
# tests
pytest>=7.0
httpx>=0.24        # fastapi TestClient transport
