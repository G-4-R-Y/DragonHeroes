# Dragon Heroes — Agent Guidelines

This file mirrors **`CLAUDE.md`** — read it first; it is the same contract
(canon first, standing directives, hard rules, session hygiene, layout,
resume procedure via `docs/harness/`).

## The roadmap rule (Ricardo, 2026-09-12)

**`docs/harness/20-roadmap.md` is the single demand ledger.** Since
2026-09-21 it carries ONLY the ledger — the narrative behind each row lives
in `docs/harness/21-work-journal.md`, moved verbatim, nothing summarized
away. New demands still land in the roadmap; long write-ups go to the
journal so the ledger stays readable. Always:

1. **Add every new demand to the roadmap the moment it arrives** — feature,
   bug, tweak, question-to-investigate — with status and a pointer to its law
   doc, before starting the work.
2. **Work item by item from the roadmap**, oldest NOW items first unless
   Ricardo reprioritizes; mark items done (with the gate that proves it) in
   the same change as the code.
3. **At interruptions** (new message, pivot, compaction): first append the
   new demands/details to the roadmap — plus the current strategies/plans
   state — then continue. Nothing asked is ever lost, even mid-sentence.
